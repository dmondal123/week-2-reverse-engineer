# Risk / Control (Failure–Recovery) table

Generated from `artifacts/results/failure-injection.json`, `evidence/ledger.csv`, and the release-control source (`src/rag_compare/release.py`). Each control is framework-independent: it sits outside both frameworks by design (H3).

| # | Risk | Trigger scenario | Control | Where it lives | Evidence it works | Residual risk / unknown |
|---|---|---|---|---|---|---|
| R1 | Mixed-version answer after partial reindex | Staging namespace holds half of v2 while manifest claims all five documents | `validate_release` compares observed file set, per-file SHA-256, counts, schema version, and embedding identity against the manifest; promotion refused unless validation_status == "passed" | Release layer (`src/rag_compare/release.py`) | Failure injection run: partial v2 rejected with "corpus file set does not match the release directory" (`failure-injection.json` assertion `partial_promotion_rejected`) | Validation covers files/manifests; semantic drift inside a correctly-hashed file is out of scope by design |
| R2 | Pointer corruption on failed promotion | Crash or rejection mid-promotion leaves active.json half-written or pointing at the bad release | Failed validation never touches the pointer; successful promotion writes a temp file in the same directory, flush+fsync, then one atomic `os.replace` | Release layer | Assertions `pointer_unchanged_after_failure` and `complete_v2_validated_and_promoted` both true; pointer before/after recorded in result JSON | Host filesystem without atomic-rename guarantees would weaken this (not exercised) |
| R3 | Foreign-release chunks entering a query mid-flight | Active release changes between query start and retrieval completion | Release captured exactly once at query start; every retrieval branch filtered by `filter_chunks_for_release(captured_release)`, which raises `MixedReleaseError` on any foreign chunk | Shared adapter contract + both adapters | Assertions `phase_a_all_results_v1` / `phase_a_no_v2_chunk_returned` / `mixed_release_filter_raises`; release_consistency = 1.0 in all 20 controlled runs | Filter covers returned candidates; any future code path bypassing the shared filter would need a new test |
| R4 | Deleted content resurfacing after promotion | v1 namespace removed post-promotion but stale index still queried | Post-promotion queries served only from the promoted release's namespace; removed v1 chunk (office-snacks) absent from all Phase B contexts | Release layer + adapter filtering | Assertion `deleted_v1_content_not_returned` true | Only tested for the office-snacks distractor, not exhaustively for all v1 docs |
| R5 | Hidden ambient configuration changing results between builds | LlamaIndex global `Settings` fallback silently swaps embed/LLM/chunking (EV-T4-002/003/004) | Every retrieval-affecting value set explicitly and snapshot of resolved settings emitted into each stage event | Adapters (both) | Task 6 control validation passed — build manifests identical within each framework pair (`control_validation.failures = []`) | Snapshot correctness depends on adapter coverage of all ambient reads (audited in Task 4 provenance matrix, not formally proven exhaustive) |
| R6 | Identity drift at split time | Haystack splitter regenerates child ids from content+embedding fingerprint; LlamaIndex drops spans (EV-T4-007/008, LI-9) | External stable-identity contract: document_id = SHA-256(source_id, bytes), chunk_id = SHA-256(doc_id, span, text); adapters normalize to this contract regardless of native ids | Identity layer (`src/rag_compare/identity.py`) | Provenance matrix shows native ids transformed/synthesized while normalized candidate fields carry contract ids; citations resolve to corpus spans (citation_span_correctness = 1.0) | Contract ids are assigned by the wrapper, so they are only as correct as the span bookkeeping around native parsers |
| R7 | Wrong model identity recorded in manifests | Task 1 recorded `-hf` model tags that never existed on the server | Ledger supersession: EV-T2-003/004 → EV-T5-001/002 with live tags + full digests verified via `/api/tags` and `/api/embed` | Environment manifest + ledger | Digests pinned in `config/experiment.json` models block; experiment ran against live digests | None known beyond single-host verification |
