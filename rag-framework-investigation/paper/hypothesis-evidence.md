# Hypothesis / Evidence table

Generated from `evidence/hypotheses.md` and `evidence/ledger.csv` (not from memory). Status values are exactly as recorded in the hypotheses file.

| ID | Hypothesis (pre-trace) | Prediction summary | Rejection condition | Key evidence (IDs) | Runtime/artifact evidence | Status | Confidence before → after |
|---|---|---|---|---|---|---|---|
| H1 | LlamaIndex hidden convenience state | Global `Settings` and convenience defaults conceal retrieval-affecting choices unless each resolved value is captured | Traced paths show no material global/default reads, or every read is overridden and emitted in traces | EV-T4-002 (`base.py:71` embed fallback), EV-T4-003 (`constants.py:10,12` DEFAULT_CHUNK_SIZE/DEFAULT_SIMILARITY_TOP_K), EV-T4-004 (`settings.py:19-291` singleton), EV-T4-005 (synthesizer `Settings.llm`), REF-T4 | Task 6 adapters pass every value explicitly and snapshot resolved settings into stage events; control validation passed with zero manifest differences beyond retriever (`artifacts/results/controlled-summary.json` `control_validation.passed=true`) | OPEN — mechanism confirmed at source level; the adapter-level mitigation was exercised but the hypothesis as stated (about the framework) is neither rejected nor weakened | medium → high (that the hidden state exists) |
| H2 | Haystack graph visibility vs transformation risk | Explicit component graph improves observability, but converters/splitters can still invisibly transform identity/metadata | Graph does not improve observability, or every converter/splitter preserves identity without unrecorded transformation | EV-T4-010 (`pipeline.py:118,200` explicit graph), EV-T4-007 (splitter regenerates child ids, drops parent id), EV-T4-008 (id fingerprint includes embedding), EV-T4-009 (constructor-local defaults) | Provenance matrix: Haystack `document_id` index-time = transformed (HS-5), `chunk_id` synthesized (HS-4); LlamaIndex loses span_offsets entirely | **WEAKENED** (2026-08-23) — observability half holds; transformation risk is stronger than predicted: child id depends on embedding identity via `_fingerprint`, a coupling not surfaced by the graph | medium → confirmed-weakened with counterevidence (EV-T4-007, EV-T4-008) |
| H3 | Frameworks do not provide atomic reproducible releases alone | Neither framework validates a complete versioned manifest and atomically switches a query-visible pointer | Either framework natively satisfies both properties without external release logic | EV-T4-001/LI-1, EV-T4-006/HS-2, HS-2 (only DuplicatePolicy, no release concept); provenance matrix rows `corpus_release_version` = lost at index-time for both frameworks | Failure injection run `failure-injection-20260823T193859`: partial v2 promotion rejected ("corpus file set does not match the release directory"), pointer byte-identical after failure, all Phase A queries v1-only, complete v2 promoted atomically, deleted v1 content never returned (`artifacts/results/failure-injection.json`, 9/9 assertions true) | OPEN → supported by both source and runtime evidence (no native mechanism found; external release layer provided both properties) | high → high |

## Counterevidence record

- H2 counterevidence (source-level): `document_splitter.py:268,332` @ haystack@c7cb46c0 — children created as new Documents with content-derived ids, parent id dropped (EV-T4-007); `dataclasses/document.py:110-116` — id fingerprint includes the embedding vector (EV-T4-008). This is the required counterexample against the favored "graph visibility is sufficient" reading.
- Model-identity counterevidence: ledger EV-T2-003/EV-T2-004 (recorded `-hf` model tags) were contradicted and superseded by EV-T5-001/EV-T5-002 (live tags `nomic-embed-text:latest`, `qwen3:4b-instruct-2507-q4_K_M` with full digests). Recorded as superseded, not deleted.

## Rejected / weakened explanation

The pre-trace reading of H2 — that Haystack's visible pipeline graph makes identity transformations observable and therefore low-risk — is **weakened**: the graph exposes component boundaries (EV-T4-010) but does not surface that document ids silently change when embeddings or metadata change (EV-T4-008). Identity risk must be handled by the external contract, not assumed away by graph visibility.
